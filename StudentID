import 'dart:html';

import 'package:flutter/material.dart';

void main() {
  runApp(
    MaterialApp(
      home: Scaffold(
        backgroundColor: const Color(0xfff9f4f3),
        appBar: AppBar(
          title: const Text('Student Profile Card'),
          backgroundColor: Colors.deepPurpleAccent,
        ),
        body: Center(
          child: Column(
            // Changed Row to Column for proper layout
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              const SizedBox(height: 30),
              CircleAvatar(
                radius: 70,
                backgroundImage: AssetImage('assets/images/myimage.jpeg'),
              ),
              const SizedBox(height: 20), // Added spacing between icon and text
              const Text(
                "T TANUJA SINDHU",
                style: TextStyle(
                  fontSize: 30,
                  fontWeight: FontWeight.bold,
                  fontStyle: FontStyle.normal,
                ),
              ),
              const Text(
                "Roll No: 24PA1A05N0",
                style: TextStyle(
                  fontSize: 20,
                  fontWeight: FontWeight.normal,
                  fontStyle: FontStyle.normal,
                ),
              ),
              const Text(
                "Branch: CSE",
                style: TextStyle(
                  fontSize: 20,
                  fontWeight: FontWeight.normal,
                  fontStyle: FontStyle.normal,
                ),
              ),
              Container(
                width: 300,
                height: 30,
                color: Color(0xfff6c5c5), // Corrected property name
                child: Row(
                  children: [
                    Icon(
                      Icons.phone_callback_outlined,
                      color: Color(0xff272222),
                      size: 20,
                    ),
                    SizedBox(width: 8),
                    const Text(
                      // Added const for better performance
                      "91 1234567890",
                      style: TextStyle(
                        fontSize: 20,
                        fontWeight: FontWeight.normal,
                        fontStyle: FontStyle.normal,
                      ),
                    ),
                  ],
                ),
              ),
              SizedBox(height: 10),
              Container(
                width: 300,
                height: 30,
                color: Color(0xfff6c5c5), // Corrected property name
                child: Row(
                  children: [
                    Icon(
                      Icons.email,
                      color: Color(0xff272222),
                      size: 20,
                    ),
                    SizedBox(width: 8),
                    const Text(
                      // Added const for better performance
                      "tanujasindhu@gmail.com",
                      style: TextStyle(
                        fontSize: 20,
                        fontWeight: FontWeight.normal,
                        fontStyle: FontStyle.normal,
                      ),
                    ),
                  ],
                ),
              ),
            ],
          ),
        ),
      ),
    ),
  );
}
